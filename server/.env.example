PORT=8080

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=gyeongjuex

JWT_SECRET=
JWT_ACCESS_EXPIRES_IN=1h
JWT_REFRESH_EXPIRES_IN=14d

# 미션 인증 사진 업로드용 S3 버킷.
AWS_REGION=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET=
# CloudFront 등 커스텀 도메인으로 서빙한다면 설정 (없으면 버킷의 기본 S3 URL 사용).
AWS_S3_PUBLIC_BASE_URL=
